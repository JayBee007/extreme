version: "3.7" 

services:
  web:
    build: .
    volumes:
      - type: bind
        source: ./
        target: /app
    ports:
      - "3030:3030"
    links:
      - "db"
    depends_on:
      - "db"
    networks: 
      - "chat"

  db:
    image: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=chat
    ports:
      - "5432:5432"
    networks: 
      - "chat"


networks: 
  chat:
